apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels:
    app: react-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
      - name: react-app
        image: mcorries/react-app:latest
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: private-dockerhub-secret
